on: [push]
jobs:
    build_and_static_analysis:
      runs-on: ubuntu-latest
      container:
        image: logavaseekar/cs537-tester:latest
      steps:
        - name: Checkout the repo
          uses: actions/checkout@v3
        - run: cd p1 && make
        - run: cd p1 && make debug
